I have updated my OpenAPI spec at inputs/api_reference/openapi.json. Regenerate the API reference docs for my Docusaurus website.

## Context

This project uses `docusaurus-plugin-openapi-docs` and `docusaurus-theme-openapi-docs` to render interactive API reference pages from the OpenAPI spec.

The API docs live in a **separate docs plugin instance** (`api-docs/`) to avoid
conflicting with the autogenerated sidebar used by the main `docs/` directory.

- OpenAPI input spec: inputs/api_reference/openapi.json
- Shared introduction input source: inputs/api_reference/introduction.md
- REST API introduction input source: inputs/api_reference/rest_api_introduction.md
- Python Client introduction input source: inputs/api_reference/python_client_introduction.md
- Python Client resource input source directory: inputs/api_reference/python_client/
- Version input source: inputs/version.txt
- Generated output directory: api-docs/ (gitignored — fully regenerated each time)
- Generated sidebar: api-docs/sidebar.ts
- Generated REST intro page: api-docs/backend-api.info.mdx (route: /api/backend-api)
- Shared API intro page: api-docs/api-reference-overview.mdx (route: /api/api-reference-overview)

## Steps

1. Remove the api-docs/ directory if it exists:

   rm -rf api-docs

2. Create a fresh empty directory:

   mkdir -p api-docs

3. The `@docusaurus/plugin-content-docs` instance for api-docs requires
   `sidebarPath` to point to an existing file at startup. But the sidebar
   doesn't exist yet (it's generated in the next step). Temporarily set
   `sidebarPath` to `false` in docusaurus.config.ts:

   Change:  sidebarPath: './api-docs/sidebar.ts',
   To:      sidebarPath: false,

4. Generate the API docs from the spec:

   npx docusaurus gen-api-docs all

5. Create and customize API introduction and Python Client pages:

   5a) Create or update a shared API introduction page:

   - Read shared intro markdown from: `inputs/api_reference/introduction.md`.
   - Read the version string from: `inputs/version.txt`.
   - Create or update file: `api-docs/api-reference-overview.mdx`
   - Use this frontmatter:

     ---
     id: api-reference-overview
     title: "DeepAlphaResearch.ai API Reference"
     sidebar_label: Introduction
     sidebar_position: 0
     ---

   - Add a visible H1 at the top of the page:
     `# DeepAlphaResearch.ai API Reference`
   - Add a version line directly below the H1:
     `Version: <value from inputs/version.txt>`
   - Insert the full body from `inputs/api_reference/introduction.md` below the
     version line.
   - If shared intro content already exists in this file, replace it with the
     latest markdown from `inputs/api_reference/introduction.md`.
   - Preserve valid MDX formatting in the final file.

   5b) Customize the generated REST API intro page:

   - Read REST intro markdown from: `inputs/api_reference/rest_api_introduction.md`.
   - Read the version string from: `inputs/version.txt`.
   - Target generated file: `api-docs/backend-api.info.mdx`
   - Keep the generated `id`, `sidebar_label`, `sidebar_position`, `hide_title`,
     `custom_edit_url`, and imports unchanged.
   - Set the page frontmatter title to:
     `title: "REST API Reference"`
   - Set the visible H1 heading to:
     `children={"REST API Reference"}`
   - Set the version badge content to:
     `children={"Version: <value from inputs/version.txt>"}`
   - Insert the REST intro markdown body immediately after the closing
     `</Heading>` line.
   - If intro markdown already exists there, replace it with the new content from
     `inputs/api_reference/rest_api_introduction.md`.
   - Preserve valid MDX formatting in the final file.

  5c) Create or update the Python Client landing page from markdown:

  - Read Python Client intro markdown from:
    `inputs/api_reference/python_client_introduction.md`.
  - Read the version string from: `inputs/version.txt`.
  - Create or update file: `api-docs/python-client-reference.mdx`
  - Use this frontmatter:

    ---
    id: python-client-reference
    title: "Python Client Reference"
    sidebar_label: Introduction
    sidebar_position: 0
    ---

  - Add a visible H1 at the top of the page:
    `# Python Client Reference`
  - Add a version line directly below the H1:
    `Version: <value from inputs/version.txt>`
  - Insert the full body from
    `inputs/api_reference/python_client_introduction.md` below the version line.
  - If intro content already exists in this file, replace it with the latest
    markdown from `inputs/api_reference/python_client_introduction.md`.
  - Preserve valid MDX formatting in the final file.

  5d) Create or update Python Client resource pages from
  `inputs/api_reference/python_client/`:

  - Source-to-target file mapping:
    - `agent.md` -> `api-docs/python-client-agent.mdx`
    - `experiments.md` -> `api-docs/python-client-experiments.mdx`
    - `market_data.md` -> `api-docs/python-client-market-data.mdx`
    - `strategies.md` -> `api-docs/python-client-strategies.mdx`
    - `trials.md` -> `api-docs/python-client-trials.mdx`
    - `models.md` -> `api-docs/python-client-models.mdx`
  - For each target file, set stable frontmatter:
    - `agent.md`: id `python-client-agent`, title `Agent API`
    - `experiments.md`: id `python-client-experiments`, title `Experiments API`
    - `market_data.md`: id `python-client-market-data`, title `Market Data API`
    - `strategies.md`: id `python-client-strategies`, title `Strategies API`
    - `trials.md`: id `python-client-trials`, title `Trials API`
    - `models.md`: id `python-client-models`, title `Models`
  - Copy each source markdown body into its target MDX file (excluding the
    source file's top-level `# ...` title since title is already in frontmatter).
  - Preserve all section headings, tables, and fenced code blocks.
  - If any source file links to another markdown page, normalize links to MDX/doc
    routes so they resolve in Docusaurus.
  - The generation must be idempotent: re-running replaces stale content and must
    not duplicate sections.

6. Restore `sidebarPath` in docusaurus.config.ts:

   Change:  sidebarPath: false,
   To:      sidebarPath: './api-docs/sidebar.ts',

7. Normalize the generated API sidebar into grouped folders:

   - Target file: `api-docs/sidebar.ts`
   - Keep all generated endpoint docs and categories, but wrap them under a stable
     root `apisidebar` with API-reference folders.
   - The final exported value must always be a valid `SidebarsConfig` object.
   - Never export a single sidebar array (for example,
     `export default sidebar.apisidebar;` is invalid).
   - This normalization must be idempotent (safe to apply on each regeneration
     without adding duplicate wrappers).

   Use this target shape:

      import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

      const generatedSidebar: SidebarsConfig = { ...generated content... };

      const restApiItems =
        generatedSidebar.restApi ??
        generatedSidebar.backend ??
        generatedSidebar.apisidebar ??
        [];

      const pythonClientItems = [
        {
          type: "doc",
          id: "python-client-reference",
          label: "Introduction",
        },
        {
          type: "doc",
          id: "python-client-agent",
          label: "Agent API",
        },
        {
          type: "doc",
          id: "python-client-experiments",
          label: "Experiments API",
        },
        {
          type: "doc",
          id: "python-client-market-data",
          label: "Market Data API",
        },
        {
          type: "doc",
          id: "python-client-strategies",
          label: "Strategies API",
        },
        {
          type: "doc",
          id: "python-client-trials",
          label: "Trials API",
        },
        {
          type: "doc",
          id: "python-client-models",
          label: "Models",
        },
      ];

      const sidebar: SidebarsConfig = {
        apisidebar: [
          {
            type: "doc",
            id: "api-reference-overview",
            label: "Introduction",
          },
          {
            type: "category",
            label: "REST API",
            collapsible: true,
            collapsed: false,
            items: restApiItems,
          },
          {
            type: "category",
            label: "Python Client",
            collapsible: true,
            collapsed: false,
            items: pythonClientItems,
          },
        ],
      };

      export default sidebar;

8. Verify intro content, grouped sidebar behavior, and site build:

   - Open `api-docs/api-reference-overview.mdx` and confirm:
     - frontmatter id is `api-reference-overview`
     - title and H1 are `DeepAlphaResearch.ai API Reference`
     - it includes the version value from `inputs/version.txt`
     - body contains the exact text from `inputs/api_reference/introduction.md`
   - Open `api-docs/backend-api.info.mdx` and confirm:
     - frontmatter title is `REST API Reference`
     - heading is `REST API Reference`
     - version badge uses the value from `inputs/version.txt`
     - body contains the exact text from `inputs/api_reference/rest_api_introduction.md`
       after the `</Heading>` block
   - Open `api-docs/python-client-reference.mdx` and confirm:
     - frontmatter id is `python-client-reference`
     - title and H1 are `Python Client Reference`
     - it includes the version value from `inputs/version.txt`
     - body contains the exact text from
       `inputs/api_reference/python_client_introduction.md`
   - Open each generated Python Client page and confirm it exists with the expected
     stable ID and title:
     - `api-docs/python-client-agent.mdx` (`python-client-agent`, `Agent API`)
     - `api-docs/python-client-experiments.mdx` (`python-client-experiments`,
       `Experiments API`)
     - `api-docs/python-client-market-data.mdx` (`python-client-market-data`,
       `Market Data API`)
     - `api-docs/python-client-strategies.mdx` (`python-client-strategies`,
       `Strategies API`)
     - `api-docs/python-client-trials.mdx` (`python-client-trials`, `Trials API`)
     - `api-docs/python-client-models.mdx` (`python-client-models`, `Models`)
     - each body preserves markdown sections/tables/code blocks from its source file
       in `inputs/api_reference/python_client/`
   - Open `api-docs/sidebar.ts` and confirm:
     - it exports a `SidebarsConfig` object (not a sidebar array)
     - top-level `apisidebar` starts with `api-reference-overview` Introduction
     - top-level `apisidebar` contains a `REST API` category
     - all currently generated REST endpoint docs remain reachable under `REST API`
     - top-level `apisidebar` contains a `Python Client` category with:
       `python-client-reference`, `python-client-agent`,
       `python-client-experiments`, `python-client-market-data`,
       `python-client-strategies`, `python-client-trials`,
       `python-client-models`
   - Confirm route `/api/api-reference-overview` shows the shared intro content.
   - Confirm route `/api/backend-api` shows the REST intro content.
   - Confirm route `/api/python-client-reference` shows the Python Client
     introduction content.

   npx docusaurus build

## Setup (only needed if the plugin is not yet installed)

If the packages `docusaurus-plugin-openapi-docs` and `docusaurus-theme-openapi-docs` are not in package.json, run:

   npm install docusaurus-plugin-openapi-docs docusaurus-theme-openapi-docs

Then make the following configuration changes:

### docusaurus.config.ts

1. Add the theme to the `themes` array:

   themes: ['@docusaurus/theme-mermaid', 'docusaurus-theme-openapi-docs'],

2. Add a top-level `plugins` array with both the OpenAPI docs generator and a
   separate docs plugin instance for the API pages. REST API endpoint docs are
   generated from OpenAPI, while Python Client docs are created from markdown
   sources in `inputs/api_reference/python_client/` and
   `inputs/api_reference/python_client_introduction.md`:

   plugins: [
     [
       'docusaurus-plugin-openapi-docs',
      {
        id: 'api',
        docsPluginId: 'api-docs',
        config: {
          restApi: {
            specPath: 'inputs/api_reference/openapi.json',
            outputDir: 'api-docs',
            sidebarOptions: {
              groupPathsBy: 'tag',
            },
          },
        },
      },
     ],
     [
       '@docusaurus/plugin-content-docs',
       {
         id: 'api-docs',
         path: 'api-docs',
         routeBasePath: 'api',
         docItemComponent: '@theme/ApiItem',
         sidebarPath: './api-docs/sidebar.ts',
       },
     ],
   ],

   IMPORTANT: Do NOT add `docItemComponent` to the preset's docs config.
   It belongs only on the `api-docs` plugin instance above so that regular
   docs pages in docs/ are not affected.

3. Add an "API Reference" navbar item pointing to the separate plugin instance:

   items: [
     {
       type: 'docSidebar',
       sidebarId: 'tutorialSidebar',
       position: 'left',
       label: 'Documentation',
     },
     {
       to: '/api/api-reference-overview',
       position: 'left',
       label: 'API Reference',
     },
   ],

   NOTE: The link must point to `/api/api-reference-overview` (the shared intro page),
   not `/api`, because the plugin does not generate an index page at the root.

### sidebars.ts

No changes needed. The main sidebars.ts continues to use the autogenerated
sidebar for docs/ only. The API sidebar is handled by api-docs/sidebar.ts
which is generated automatically and referenced by the api-docs plugin instance.
